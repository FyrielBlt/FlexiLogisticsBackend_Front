import { Middleware } from './MiddlewareTypes';
import { Router, Vue } from './VueTypes';
export declare type Install<T> = (vue: Vue, router?: T) => void;
export interface PluginOptions {
    router: Router;
    middleware?: Middleware[] | Middleware;
    context?: {
        [key: string]: any;
    };
}
