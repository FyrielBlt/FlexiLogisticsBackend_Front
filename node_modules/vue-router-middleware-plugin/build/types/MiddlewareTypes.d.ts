import { RouteContext } from './VueTypes';
export declare type Middleware = (context: RouteContext) => void;
export interface MiddlewarePipeline {
    ignore?: Middleware[] | Middleware;
    attach?: Middleware[] | Middleware;
}
