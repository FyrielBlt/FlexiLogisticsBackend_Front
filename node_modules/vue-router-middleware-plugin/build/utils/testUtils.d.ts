import 'regenerator-runtime/runtime';
import { RouteHook, Router } from '../types/VueTypes';
export declare const expectErrorClass: (fn: () => void, ErrorClass: unknown) => Promise<void>;
export declare class VueRouter implements Router {
    routeHook: RouteHook;
    beforeEach(routeHook: RouteHook): void;
}
