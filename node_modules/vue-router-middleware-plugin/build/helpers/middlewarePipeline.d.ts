import 'regenerator-runtime/runtime';
import { Middleware } from '../types/MiddlewareTypes';
import { RouteContext } from '../types/VueTypes';
export declare const middlewarePipeline: (context: RouteContext, middlewares: Middleware[]) => Promise<void>;
